# Auto detect text files and perform LF normalization
* text=auto

# Force LF line endings for shell scripts and Unix files
*.sh text eol=lf
*.bash text eol=lf
mongo-init.sh text eol=lf

# Force LF for Dockerfile and Docker Compose files
Dockerfile* text eol=lf
docker-compose*.yml text eol=lf

# Force LF for configuration files that might be used in containers
*.conf text eol=lf
nginx.conf text eol=lf

# Force LF for SQL files that might be executed in containers
*.sql text eol=lf
